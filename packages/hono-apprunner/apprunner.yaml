version: 1.0
runtime: nodejs22
build:
  commands:
    build:
      - echo "Installing dependencies"
      - npm install -g pnpm
      - pnpm install
      - echo "Building application"
      - pnpm build
run:
  command: pnpm start
  network:
    port: 8080
  env:
    - name: PORT
      value: "8080"
